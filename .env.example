# Telegram Bot (получить у @BotFather)
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_NAME=

# Database (для Prisma; используется при docker-compose)
# connect_timeout — не зависать при недоступной БД (секунды)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/prompt_bot"

# Порт приложения (опционально)
PORT=3000

# Swagger UI path (опционально)
# SWAGGER_PATH=/api/docs
